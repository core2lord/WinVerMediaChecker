<Window
    x:Class="WinVerMediaChecker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WinVerMediaChecker"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="WinVerMediaChecker v1.0"
    Width="500"
    Height="650"
    MinWidth="500"
    MinHeight="650"
    Background="SlateGray"
    Loaded="Window_Loaded"
    ResizeMode="CanMinimize"
    WindowStyle="ToolWindow"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Height="auto"
            Margin="5"
            Padding="5"
            Header="Step 1: Drive List">

            <StackPanel>
                <TextBlock
                    FontFamily="Consolas"
                    FontSize="16"
                    FontWeight="UltraLight"
                    Text="After media has been inserted, click the refresh button below, then select the drive letter Windows assigned using this drop-down menu."
                    TextAlignment="Center"
                    TextWrapping="WrapWithOverflow" />
                <ComboBox
                    x:Name="ActiveDrivesList"
                    Margin="15"
                    SelectionChanged="ActiveDrivesList_SelectionChanged" />
                <Button
                    x:Name="RefreshDrivesButton"
                    Margin="15,1"
                    Padding="10"
                    HorizontalAlignment="Right"
                    Click="RefreshDrivesButton_Click"
                    ClickMode="Press"
                    Content="&#xE149;"
                    FontFamily="Segoe Fluent Icons"
                    FontSize="22"
                    FontWeight="Bold"
                    ToolTip="Refresh drive list" />
            </StackPanel>
        </GroupBox>
        <Separator
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Margin="5"
            VerticalAlignment="Top"
            RenderTransformOrigin="0.5,0.5">
            <Separator.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleY="5" />
                    <SkewTransform />
                    <RotateTransform />
                    <TranslateTransform />
                </TransformGroup>
            </Separator.RenderTransform>
        </Separator>
        <GroupBox
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Margin="5"
            Padding="5"
            Header="Step 2: Confirm Selection">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label
                        Margin="5"
                        Padding="10"
                        Content="Selected Drive to Check:"
                        FontFamily="Consolas"
                        FontSize="16" />
                    <Border
                        Margin="-10,3"
                        BorderBrush="Black"
                        BorderThickness="3"
                        CornerRadius="2">
                        <Label
                            x:Name="DriveSelection"
                            MinWidth="40"
                            Background="Beige"
                            FontFamily="Consolas"
                            FontSize="16"
                            FontWeight="Bold" />
                    </Border>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>